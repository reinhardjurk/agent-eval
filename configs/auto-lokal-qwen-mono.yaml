# Zelle 3: Agent-Variation — EIN Universalagent statt drei Spezialisten
id: auto-lokal-qwen-mono
model: ollama/qwen3.6:35b-mlx

agents:
  - cards/car-agent.json

mcp:
  - server: mcp/car-server.yaml

context:
  system_prompt: prompts/car-concierge.md
  customer_context: none

sampling:
  max_tokens: 2048

simulator:
  model: ollama/gemma4:latest

repetitions: 1
